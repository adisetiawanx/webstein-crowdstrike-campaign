<?php
/**
 * CrowdStrike and Mimecast co-brand lockup.
 *
 * PLACEHOLDER. The real logo files have not been supplied yet, see issue #3.
 * They will not be traced out of the mockup. Request vector or high resolution
 * PNG for both marks from Excelerate, drop them in assets/images/, and replace
 * the two placeholder spans below.
 *
 * @package crowdstrike-campaign
 */

defined( 'ABSPATH' ) || exit;
?>
<div class="csc-lockup">
	<span class="csc-lockup__mark csc-placeholder" role="img" aria-label="CrowdStrike logo, placeholder pending the supplied asset">
		CROWDSTRIKE
	</span>

	<span class="csc-lockup__rule" aria-hidden="true"></span>

	<span class="csc-lockup__mark csc-placeholder" role="img" aria-label="Mimecast logo, placeholder pending the supplied asset">
		mimecast
	</span>
</div>
